#include <sys/time.h>
#include <stdio.h>

double setTimer(){
    double ti, tf, tempo;
    ti = tf = tempo = 0;
    struct timeval tempo_inicio, tempo_fim;
    gettimeofday(&tempo_inicio, NULL);
    
}


